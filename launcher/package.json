{
  "name": "vision-dataset-studio-launcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --no-bundle",
    "bundle": "tauri build --bundles app --config src-tauri/tauri.macos.conf.json",
    "bundle:all": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
